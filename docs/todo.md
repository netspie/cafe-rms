# TODO

- Add order cancellation reason
- During an event, keep a standard menu available for customers not participating in the event
- Make `CreatedBy` non-nullable
- Extract API route layer (separate route definitions from use case handlers)
